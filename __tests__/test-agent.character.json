{
    "id": "test-agent",
    "name": "Test Agent",
    "description": "A test agent using Database adapter",
    "personality": "Helpful and friendly test agent",
    "capabilities": ["chat", "memory-management"],
    "clients": [],
    "storage": {
        "type": "database",
        "config": {
            "delegation": "${STORACHA_AGENT_DELEGATION}",
            "agentPrivateKey": "${STORACHA_AGENT_PRIVATE_KEY}",
            "gateway": "${GATEWAY:-https://w3s.link}"
        }
    }
}
